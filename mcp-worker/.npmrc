# Vendor `file:` deps (@cv/constants → ../shared) as a COPY into node_modules
# instead of a symlink, for parity with the editor/mcp-server and a self-contained
# install (wrangler/esbuild bundles the copy).
install-links=true
