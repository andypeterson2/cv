# Vendor `file:` deps (i.e. @cv/constants → ../shared) as a COPY into node_modules
# instead of a symlink, so the multi-stage Docker `deploy` image is self-contained
# (node_modules carries @cv/constants; no /app/shared needed at runtime).
install-links=true
